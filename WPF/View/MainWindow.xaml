<Window x:Class="WPF.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid Margin="10" Background="DimGray">
        <StackPanel>
            <Grid Margin="10,10,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="Food: " x:Name="FoodText" Grid.Column="0" 
                           HorizontalAlignment="Stretch" TextAlignment="Left" FontSize="20"/>
                <TextBlock Text="Wood: " x:Name="WoodText" Grid.Column="1" 
                           HorizontalAlignment="Stretch" TextAlignment="Left" FontSize="20"/>
                <TextBlock Text="Stone: " x:Name="StoneText" Grid.Column="2" 
                           HorizontalAlignment="Stretch" TextAlignment="Left" FontSize="20"/>
                <TextBlock Text="Gold: " x:Name="GoldText" Grid.Column="3" 
                           HorizontalAlignment="Stretch" TextAlignment="Left" FontSize="20"/>
                <TextBlock Text="Population: " x:Name="PopulationText" Grid.Column="4" 
                           HorizontalAlignment="Stretch" TextAlignment="Left" FontSize="20"/>
            </Grid>
            
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Content="Collect Food" Click="CollectFood_Click" Margin="5"
                        Padding="10,5"
                        FontSize="16"
                        Background="DarkGreen"
                        Foreground="White"
                        BorderBrush="Black"
                        BorderThickness="2"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="ForestGreen"/>
                                </Trigger>
                                <!-- Pressed -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="Green"/>
                                </Trigger>
                                <!-- Disabled -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="Gray"/>
                                    <Setter Property="Foreground" Value="LightGray"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="Collect Wood" Click="CollectWood_Click" Margin="5"
                        Padding="10,5"
                        FontSize="16"
                        Background="SaddleBrown"
                        Foreground="White"
                        BorderBrush="Black"
                        BorderThickness="2"
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <!-- Hover -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="Peru"/>
                                </Trigger>
                                <!-- Pressed -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="Sienna"/>
                                </Trigger>
                                <!-- Disabled -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="Gray"/>
                                    <Setter Property="Foreground" Value="LightGray"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <!--<Button Content="Collect Stone" Click="CollectStone_Click" Margin="5"/>
                <Button Content="Collect Gold" Click="CollectGold_Click" Margin="5"/>-->
            </StackPanel>
            
            <WrapPanel x:Name="BuildingPanel" Margin="0,20,0,0" Orientation="Vertical"/>
            
        </StackPanel>
    </Grid>
</Window>
